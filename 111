public class JPanelCat extends JPanel {

    private LayoutManager2 layoutManager2;

    public JPanelCat(LayoutManager2 layoutManager2){
        super(layoutManager2);
        this.layoutManager2 = layoutManager2;
    }

    public JPanelCat(){
        super();
    }

    public static JPanelCat jump(Function<JPanelCat,JPanelCat> function, LayoutManager2 layoutManager2){
        return function.apply(new JPanelCat(layoutManager2));
    }

    public static JPanelCat jump(Function<JPanelCat,JPanelCat> function){
        return function.apply(new JPanelCat());
    }
}
